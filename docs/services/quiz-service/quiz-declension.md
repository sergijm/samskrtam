# Спецификация квиза: Склонение санскритских существительных

> Связанные файлы: [README.md](../../README.md) · [services/quiz-service.md](../../services/quiz-service.md)
> Статус: **ЧЕРНОВИК**

---

## 1. Данные для квиза

### 1.1. Источник существительных
Существительные (основы) и их полные таблицы склонений загружаются в базу данных вручную. Для каждого существительного должны быть явно указаны его основа, тип тематической гласной и все 24 словоформы (8 падежей × 3 числа) в IAST.

**NEW:** для каждой основы дополнительно обязательны деванагари и перевод (ru/en) — см. `quiz.declension_stems` в [services/quiz-service.md §12](../services/quiz-service.md). Без этих полей UI квиза не может показать основу на деванагари и её значение (см. bug-fix: "счётчик и заголовок вопроса квиза").

**Пример исходных данных:**

---

## 2. Примечание об окончаниях для основ -i, -u, -ṛ

Для основ на **-i**, **-u**, **-ṛ** (`vowel_type`: I, I_LONG, U, U_LONG, R) падежные окончания **не различаются** по грамматическому роду ни в одном падеже/числе.
Подробнее см. [architecture.md §3.3](../architecture.md#33-хранение-окончаний-склонений).

**Ключевые следствия:**
- Ключ агрегации прогресса остаётся `(gender, caseType, numberType)` для единообразия по всем 8 урокам склонений (включая declensions-i, declensions-u, declensions-r).
- `caseEnding` для MASCULINE и FEMININE в рамках одного (caseType, numberType) совпадает.
- `successRate` считается раздельно по роду, так как это разные основы/слова.

**Местоимения (architecture.md §3.4) — тот же паттерн, без нового itemType:** уроки `pronouns-personal`/`pronouns-demonstrative`/`pronouns-interrogative`/`pronouns-relative`/`pronouns-reflexive` используют `DECLENSION_FORM` наравне с обычными основами. `pronouns-personal` (aham/tvam) — `gender = UNSPECIFIED`, как i/u/ṛ-основы выше. `pronouns-demonstrative`/`interrogative`/`relative` (tad/etad/idam/kim/yad) — по 3 стема на класс (MASCULINE/FEMININE/NEUTER), как a-основы одного рода (architecture.md §3.3). `pronouns-reflexive` (ātman) — обычная a-основа муж. рода, данные дублируют `declensions-a-masc` под отдельным slug (сознательный выбор, см. architecture.md §3.4). Энклитики и несклоняемое `svayam` — вне контракта DECLENSION_FORM.

---

## 3. UI стартовой страницы квиза: таблицы прогресса и мульти-выбор

**Статус: ЧЕРНОВИК (обновлено при постановке задачи Агентам 2/3/6, ревизия 2).**

### 3.1. Три вкладки прогресса + вкладка «Сессии»

> ⚠️ **Расхождение состава вкладок, зафиксировано Агентом 6.** Актуальный состав и порядок вкладок этой же страницы (`GrammarLessonPage`, `/lessons/grammar/:type`) — в более новом `frontend/pages/grammar-lesson-page.md` §2: Статистика → **Парадигмы** → По падежам → По числам → Подробно (вкладка «Сессии» там не описана). Список ниже (4 вкладки) не обновлялся при выделении `grammar-lesson-page.md` и по составу устарел — не редактируется в рамках текущей задачи (вне scope), но новая вкладка **«Парадигмы»** (реальные словоформы из `content.declension_forms`, а не абстрактные окончания) описана только там: `grammar-lesson-page.md` §2.2 `GrammarParadigmTable`, API — `curriculum-service.md` §5а `GET /content/public/lessons/{slug}/declension-paradigms`. Свести оба списка воедино — открытый вопрос, см. `README.md` §7.

Компонент — `TabView` с четырьмя вкладками:

1. **«По падежам»** — строка = один падеж (caseType), агрегат по всем numberType и gender. Колонки: чекбокс, Падеж, Изучено.
2. **«По числам»** — строка = одно число (numberType: sg/du/pl), агрегат по всем caseType и gender. Колонки: чекбокс, Число, Изучено.
3. **«Подробно»** — строка = (caseType, numberType, gender). Колонки: чекбокс, Падеж, Число, Род, Окончание, Изучено.
4. **«Сессии»** — история сессий квиза текущего урока, см. §4.

Отдельной колонки «Статус» на вкладках 1–3 больше нет — числового значения в колонке «Изучено» (`nSuccess/nAll` либо score, см. `services/quiz-service.md`) достаточно для отображения прогресса. Вместо неё — первая колонка с чекбоксом множественного выбора строк (`DataTable selectionMode="multiple"`).

### 3.2. Агрегация прогресса

Значение «Изучено» на вкладках «По падежам» и «По числам» — агрегат всех строк подробной таблицы с соответствующим caseType/numberType (по всем остальным измерениям). Способ агрегации (среднее / доля выученных комбинаций / min) — единый для обеих вкладок, определяет Агент 2 при реализации на основе уже существующей модели прогресса (см. §2 и `services/quiz-service.md`) и указывает выбор явно в реализации и в этом документе постфактум.

### 3.3. Мульти-выбор строк и запуск квиза по прогресс-сету

- Выбор чекбоксов независим для каждой из трёх вкладок прогресса (переключение вкладки не сбрасывает и не объединяет выбор с другой вкладкой).
- Кнопка **«Начать квиз»** запускает квиз только по комбинациям, соответствующим строкам, отмеченным чекбоксом **на активной в данный момент вкладке**. Если на активной вкладке не отмечена ни одна строка — берутся все строки этой вкладки (т.е. весь урок).
- На кнопке отображается динамический счётчик **N** — количество итоговых (caseType, numberType, gender)-комбинаций (progressTag), которые войдут в квиз (т.е. развёрнутое произведение, а не число отмеченных строк): `Начать квиз ({N})`. Пересчитывается при каждом изменении выбора на активной вкладке.
  - На вкладке «Подробно» N — это просто количество выбранных строк (или всех строк вкладки, если выбор пуст) — там строка уже равна одному progressTag.
  - На вкладках «По падежам» и «По числам» строка агрегирует несколько измерений, поэтому N — это произведение: (кол-во выбранных падежей или, если ничего не выбрано — все 8 падежей) × (кол-во выбранных чисел или, если ничего не выбрано — все 3 числа: sg/du/pl) × (кол-во родов основы: 1 или 2, в зависимости от того, различает ли данный склоняемый тип род — см. §2). Например, для a-основы (различающей 2 рода: мужской/средний или мужской/женский в зависимости от урока) при выборе 3 чисел на вкладке «По числам» и незаданном выборе падежей: N = 8 падежей × 3 числа × 2 рода = 48.
  - Общая формула: N = |выбранные_падежи ∪ все_падежи, если пусто| × |выбранные_числа ∪ все_числа, если пусто| × |число_родов_основы|. Для основ без родового различия (см. §2, gender = UNSPECIFIED) множитель родов равен 1.
- При нажатии кнопки «Начать квиз» выбранный срез разворачивается в список progressTag текущего урока (см. §3.4) и адресуется стабильным `progressTagSetId` — параметром `start-or-resume`. Бэкенд сначала проверяет, существует ли у пользователя незавершённая (`IN_PROGRESS`) сессия по данному уроку с тем же `progressTagSetId` (см. §3.4):
  - **если существует** — квиз резюмируется (используется та же сессия, тот же прогресс по ответам, без создания новой);
  - **если не существует** — создаётся новая сессия квиза, содержащая только вопросы, отфильтрованные по этому срезу (N progressTag из счётчика на кнопке).
- Клик по значению в колонке «Изучено» одной строки — частный случай того же механизма: эквивалентен выбору только этой одной строки на данной вкладке и немедленному запуску/резюму квиза по ней.

### 3.4. Прогресс-сеты (ProgressTagSet) в старте сессии

**Признак среза сессии в `QuizSession` — РЕШЕНО: стабильный `progressTagSetId`.**

Урок как набор `ProgressTag`. `progressTag` — атомарный признак вопроса в ключе агрегации
прогресса (`(caseType, numberType, gender)`, §2; для лексики — `formIast` слова, см.
quiz-generator-spec.md §3). `ProgressTagSet` — именованное подмножество progressTag урока;
прогресс сета — средний score по входящим в него progressTag. Состав сетов и их id фиксированы
дизайн-таблицей (см. quest-engine.md §2.4, quiz-generator-spec.md §3):

- по умолчанию (любой урок): `NEW`, `LEARNING`, `MASTERED` — по статусу progressTag;
  `DIFFICULT` — ортогональная ось (ошибки/низкий score, с гистерезисом выхода);
- для склонений дополнительно: `SINGULAR`/`DUAL`/`PLURAL` — по числу;
  пары падежей `ACC_LOC`, `INS_ABL`, `GEN_LOC`, `DAT_ACC` — тренировка омонимичных окончаний
  (architecture.md §3.3);
- произвольный мульти-выбор вкладок (§3.3) — комбинация нескольких именованных сетов либо
  «объединение progressTag» по явному списку; такой срез тоже получает стабильный
  `progressTagSetId` (детерминированная сигнатура отсортированного списка тегов).

В `QuizSession` хранится одно поле `progressTagSetId` (nullable; миграция Flyway — ответственность
Агента 2). Поиск существующей незавершённой сессии для резюма — по равенству `progressTagSetId`
(параметр `start`/`start-or-resume`, см. `docs/openapi/quiz/parameters.yaml` и `quiz-sessions.yaml`).
Пустой срез при старте (ни один чекбокс не выбран) разворачивается бэкендом в полный набор
progressTag урока — тот же список, что у сета со «всеми значениями» соответствующей оси.

Контракт `start-or-resume` принимает единый `progressTagSetId` — и по статусу
(NEW/LEARNING/MASTERED/DIFFICULT), и по грамматическому признаку, через тот же механизм:
quiz-service резолвит срез в список progressTag и прокидывает его в
`CurriculumClient` запрашивает пул топика и compose; curriculum-service возвращает отфильтрованный список
вопросов. Если он пуст — quiz-service отвечает `SCOPE_FILTER_EMPTY`; это бизнес-ошибка сессии,
проверка остаётся в quiz-service, а не в curriculum-service. Причина переноса фильтрации в
compose-эндпоинт curriculum-service: фильтрация по признакам
вопроса логически относится к домену контента, а не к домену сессий квиза.

---

## 4. Типы вопросов (`questionType`)

**Статус: ЧЕРНОВИК.**

До этого раздела квиз склонений поддерживал единственный неявный тип вопроса. Ниже — три типа под общим полем `QuestionDto.questionType` (см. `openapi/quiz/schemas/session.yaml`). Смена типа вопроса — исключительно решение модуля рендеринга; она не меняет генератор отбора (`quiz-generator-spec.md` §3–4), не заводит новый `itemType` и не меняет модель `quiz_item_score` — `externalRefId` для всех трёх типов остаётся тем же `case_endings.id` (§2.1 `quiz-generator-spec.md`).

### 4.1. FORM_BY_CASE (существующий, по умолчанию)

Дано: основа (`stem`, деванагари, перевод) + падеж/число/род. Нужно выбрать правильную словоформу среди дистракторов-форм (`optionType=FORM`). Без изменений относительно текущей реализации — описан здесь только для полноты перечисления типов.

### 4.2. CASE_BY_FORM (обратная задача)

Дано: готовая словоформа той же основы (`formIast`/`formDevanagari` на уровне вопроса — не основа и не падеж). Нужно выбрать правильную тройку (падеж, число, род) среди дистракторов-комбинаций (`optionType=CASE_COMBINATION`).

**Генерация дистракторов:** правильная тройка — та, что породила данную словоформу. Дистракторы — другие тройки (caseType, numberType, gender) **этого же vowel_type**, при этом приоритет отдаётся тройкам, чьё окончание **графически совпадает или близко** к правильному (например nom./voc./acc. sg. среднего рода — классический источник омонимии) — иначе задача вырождается в угадывание по чтению. Итоговый набор: 1 верная + 3 неверных тройки (как и в FORM_BY_CASE), минимум 1 из дистракторов должен быть омонимом при наличии такового у данной основы/vowel_type, остальные — случайные тройки того же vowel_type.

**Особый случай:** для vowel_type I/I_LONG/U/U_LONG/R (architecture.md §3.3, §2 этого документа) варианты-дистракторы всегда содержат `gender: UNSPECIFIED`; сама тройка не варьирует род, только caseType/numberType.

**Локализация вариантов:** каждый `CASE_COMBINATION`-option обязан нести `caseRu/caseEn/numberRu/numberEn/genderRu/genderEn` — переиспользуются те же справочники, что и в `grammar.yaml#GrammarQuestionProgress` (не заводить второй источник переводов падежей).

**Текст объяснения для CASE_BY_FORM:** не должен переиспользовать текст FORM_BY_CASE. Для CASE_BY_FORM форма уже дана в условии, а угадывается падеж/число. Формулировка: для формы `correctFormIast` основы `stem` правильный ответ — падеж `targetCase`/число `targetNumber` (+ род). Формируется в curriculum-service генераторе веткой по `questionType`.

**Проверка ответа:** для `CASE_BY_FORM` корректность определяется сравнением выбранной тройки (`caseType`+`numberType`+`gender` выбранного `CASE_COMBINATION`-option) с эталонной тройкой вопроса. Для `ENDING_MATCH` — сравнение множеств троек, а не текста.

### 4.3. ENDING_MATCH (омонимия окончаний, multi-select)

Дано: голое окончание в IAST (`caseEnding`, например `au`), **без основы** — вопрос сознательно оторван от конкретного слова, чтобы проверять знание системы окончаний, а не запоминание конкретной парадигмы. Нужно выбрать **все** тройки (падеж, число, род), которым соответствует это окончание, из смешанного пула правильных и неправильных троек.

**Мотивация типа:** прямо тренирует омонимию, упомянутую в §2 (architecture.md §3.3) и в целом свойственную санскритской системе окончаний (напр. `au` = N/V/Ac du. во многих родах и типах основ одновременно).

**Формирование пула вариантов:**
- Все тройки (caseType, numberType, gender) данного vowel_type, чьё `caseEnding` совпадает с вопросным — все они верны и все включаются в пул (без выборочного отбрасывания корректных вариантов — иначе `caseEnding` перестаёт быть однозначно определён внутри вопроса).
- Дистракторы добираются из троек с **другим** окончанием того же vowel_type до достижения целевого размера пула. Целевой размер пула — 5–7 вариантов (меньше, чем количество верных + минимум 2 дистрактора, если верных троек мало; конкретную формулу и константу определяет Агент 2 при реализации, аналогично `maxNewPerSession` и другим константам генератора).
- Пул не обязан быть кратен количеству верных ответов — важно только, что каждая верная тройка встречается ровно один раз.

**Оценка ответа:** см. `answers.yaml#AnswerRequest/AnswerResponse` — точное совпадение множеств (`selectedOptionIds` == множество всех верных `id`), без частичного зачёта. Обоснование отсутствия partial credit: формула score (§2.5 `quiz-generator-spec.md`) рассчитана на бинарный результат правильно/неправильно на один `QuizItem`; частичный зачёт потребовал бы отдельной ветки формулы только для этого questionType, что нарушает инвариантность генератора (§5 `quiz-generator-spec.md`).

**Открытый вопрос:** если у окончания в рамках vowel_type всего одна верная тройка (нет омонимии) — включать ли такое окончание в пул ENDING_MATCH вообще, или исключать при выборе `QuizItem` для этого questionType на этапе рендеринга (вопрос тогда неотличим от банального CASE_BY_FORM с одним верным ответом). Решает Агент 2 совместно с Агентом 6 при реализации; временное решение — исключать (генерировать ENDING_MATCH только для окончаний с ≥2 верными тройками).

### 4.5. Перемешивание типов в рамках одной сессии

**Статус: ЧЕРНОВИК.**

Требование: в одном квизе вопросы разных `questionType` должны чередоваться, а не идти блоками/константным типом на весь квиз.

**Где происходит выбор типа:** в curriculum-service, в момент материализации quest item при compose. Алгоритм отбора `QuizItem` остаётся полностью независим от того, как этот item потом отрендерится — `questionType` не влияет на выбор item, только на способ его подачи.

**Алгоритм назначения (на каждый DECLENSION_FORM-вопрос независимо):**
1. Базовые веса: `FORM_BY_CASE = 0.5`, `CASE_BY_FORM = 0.3`, `ENDING_MATCH = 0.2`.
2. Если для выбранной тройки (stem, case, number) окончание имеет **менее 2** омонимичных троек (условие §4.3, шаг 2.4/3) — вес `ENDING_MATCH` обнуляется, его 0.2 добавляется к `CASE_BY_FORM` (итого `FORM_BY_CASE=0.5 / CASE_BY_FORM=0.5`).
3. Anti-repeat правило: если два предыдущих вопроса **в этой сессии** (по `questionNumber - 1` и `questionNumber - 2`) уже имели один и тот же `questionType`, этот тип временно исключается из розыгрыша для текущего вопроса (веса оставшихся типов перенормируются). Для `questionNumber <= 2` правило не применяется (недостаточно истории).
4. Бросок — обычный взвешенный `Random` (детерминированный seed не требуется — воспроизводимость сессии обеспечивается персистентностью результата, см. ниже, а не повторяемостью генератора).
5. Для `questionSource=VOCABULARY` (лексические вопросы) `questionType` не назначается вообще — там остаётся текущее поведение без изменений; шаги 1–4 применяются только когда `itemType=DECLENSION_FORM`.

**Персистентность:** `questionType` вычисляется один раз при compose (материализации в `session_questions`) и сохраняется в колонке `session_questions.question_type`. Повторный вызов resume не пере-рандомизирует тип — читается сохранённое значение.

**Не путать с распределением itemType/vowel_type в самой сессии** — это отдельный, уже существующий механизм отбора (`quiz-generator-spec.md` §3–4), он не меняется.

**Открытый вопрос:** веса 0.5/0.3/0.2 — стартовое значение без A/B-данных, менять по фидбеку без ревизии контракта (это внутренняя константа curriculum-service, не часть OpenAPI).



- **Агент 5** (этот раздел + `openapi/quiz/schemas/session.yaml`, `answers.yaml`) — контракт: `QuestionType`, полиморфный `QuestionOptionDto`, `multiSelect`, `selectedOptionIds`/`correctOptionIds`. Сделано в рамках этой ревизии документа.
- **Агент 2** — реализация:
  - перенос scope-фильтра из quiz-service в curriculum-service как `QuizScopeFilterService`, применяемый **до** обрезки по количеству вопросов
  - **(bugfix)** текст `explanationRu/explanationEn` для `CASE_BY_FORM` (см. врезку выше в этом разделе) и его согласованность с фактическим направлением вопроса;
  - **(bugfix)** проверка корректности ответа для `CASE_BY_FORM`/`ENDING_MATCH` — сравнение по тройке (падеж/число/род), а не по тексту формы (см. врезку выше);
  - расширение модуля рендеринга DECLENSION_FORM в curriculum-service для генерации CASE_BY_FORM и ENDING_MATCH вопросов и их дистракторов по правилам §4.2–4.3;
  - выбор constant/формулы размера пула ENDING_MATCH (§4.3) и её фиксация постфактум в этом документе;
  - логика сравнения множеств в `QuizSessionService.submitAnswer` для `multiSelect=true` (точное совпадение, без partial credit);
  - решение по открытому вопросу §4.3 (порог ≥2 верных троек);
  - **(§4.5) Шаг 2.7:** миграция Flyway — добавить `question_type VARCHAR NULL` в `quiz.session_questions` (curriculum-service БД не трогается — тип назначается в curriculum-service, но персистится в quiz-service вместе с остальным `session_questions`, аналогично существующим `case_type`/`number_type` в этой таблице);
  - **(§4.5) Шаг 2.8:** в curriculum-service, при материализации quest item для DECLENSION_FORM — реализовать алгоритм §4.5 (веса, anti-repeat, обнуление веса ENDING_MATCH при <2 омонимах) и заполнить `questionType`;
  - **(§4.5) Шаг 2.9:** в quiz-service — `question_type` сохраняется при compose и читается при resume без пере-розыгрыша; вызов алгоритма §4.5 — только при compose, не при resume/retake.
- **Агент 3** — фронтенд:
  - компонент выбора падежа/числа/рода (single-select, radio/список) для CASE_BY_FORM — переиспользовать локализованные лейблы `caseRu/caseEn` и т.д. из опций, не хардкодить справочник падежей во фронтенде;
  - компонент multi-select (чекбоксы) для ENDING_MATCH и сборка `selectedOptionIds` при отправке ответа;
  - ветвление рендера вопроса по `questionType` (fallback на текущий FORM_BY_CASE-рендер при отсутствии поля — обратная совместимость).
- **Агент 4** — тесты:
  - unit-тесты генерации дистракторов CASE_BY_FORM (наличие омонима среди дистракторов при его существовании; корректный UNSPECIFIED-род для vowel_type I/I_LONG/U/U_LONG/R);
  - unit-тесты формирования пула ENDING_MATCH (все верные тройки присутствуют ровно один раз, дистракторы имеют другое окончание);
  - unit-тесты `submitAnswer` для `multiSelect=true` (точное совпадение множества → верно; лишний/недостающий id → неверно; пустой `selectedOptionIds` → неверно, не 400);
  - интеграционный тест полного цикла start→answer→complete хотя бы для одного вопроса каждого нового questionType;
  - **(§4.5)** unit-тест алгоритма назначения типа: три подряд одинаковых `questionType` в сессии из ≥5 DECLENSION_FORM-вопросов не встречаются (anti-repeat); при <2 омонимов вес `ENDING_MATCH`=0 (статистическая проверка на большой выборке розыгрышей, а не точное равенство — распределение вероятностное);
  - **(§4.5)** интеграционный тест: `start` → `resume` (без ответов) → `questionType` каждого вопроса не изменился между двумя ответами API (персистентность розыгрыша).

---
---

## 5. Кросс-lesson квизы

Квиз всегда запускается в границах одного урока склонения (§3) — сквозной кросс-lesson срез по
всем гласным основам с фильтрами по гласным/числам/родам в продукт не входит.
