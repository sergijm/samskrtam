package sm.selflearn.samskrtam.curriculum.questgen.morphology;

/**
 * Grammatical cases, matching the codes used by content-service
 * {@code content.case_endings} and {@code content.declension_form}.
 */
public enum CaseType {
    NOMINATIVE,
    ACCUSATIVE,
    INSTRUMENTAL,
    DATIVE,
    ABLATIVE,
    GENITIVE,
    LOCATIVE,
    VOCATIVE
}
